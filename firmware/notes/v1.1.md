# v1.1 Firmware — Dual-Signature Matcher

The architectural design: keep the OUI check as the fast path (Phase 1), and add a service-UUID check as a fall-back path (Phase 2) for devices broadcasting in privacy mode.

